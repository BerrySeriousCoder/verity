Evaluation tooling is deferred until after the MVP. Preserve source provenance and structured run records while building features so evaluation can consume them later. No benchmark runner exists yet.
